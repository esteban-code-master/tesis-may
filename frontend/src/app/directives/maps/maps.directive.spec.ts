import { MapsDirective } from './maps.directive';

describe('MapsDirective', () => {
  it('should create an instance', () => {
    const directive = new MapsDirective();
    expect(directive).toBeTruthy();
  });
});
